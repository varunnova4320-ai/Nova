[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --omit=dev --force"]

[start]
cmd = "mkdir -p meshcentral-data && ([ ! -f meshcentral-data/config.json ] && cp meshcentral-default-config.json meshcentral-data/config.json || true) && node meshcentral.js"
